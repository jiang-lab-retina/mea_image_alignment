# Image_MEA_Dulce Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-11-04

## Active Technologies
- Python 3.10+ + PyQt6 (GUI framework), NumPy (array operations), OpenCV (image stitching algorithms), czifile (CZI format I/O), tifffile (TIFF format I/O), Pillow (image utilities) (002-chip-image-stitch)
- JSON files (alignment parameters stored alongside stitched images in same directory) (002-chip-image-stitch)
- Python 3.x + PyQt6 (GUI), NumPy, OpenCV (image ops), tifffile (I/O), pathlib, json (001-corner-annotation)
- Files only (stitched images and sidecar JSON) (001-corner-annotation)

- Python 3.10+ + PyQt6 (GUI), NumPy (array operations), OpenCV (stitching), czifile (CZI format), tifffile (TIFF/LSM formats), Pillow (image I/O) (001-nsew-image-stitcher)

## Project Structure

```text
src/
tests/
```

## Commands

cd src [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] pytest [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] ruff check .

## Code Style

Python 3.10+: Follow standard conventions

## Recent Changes
- 001-corner-annotation: Added Python 3.x + PyQt6 (GUI), NumPy, OpenCV (image ops), tifffile (I/O), pathlib, json
- 002-chip-image-stitch: Added Python 3.10+ + PyQt6 (GUI framework), NumPy (array operations), OpenCV (image stitching algorithms), czifile (CZI format I/O), tifffile (TIFF format I/O), Pillow (image utilities)

- 001-nsew-image-stitcher: Added Python 3.10+ + PyQt6 (GUI), NumPy (array operations), OpenCV (stitching), czifile (CZI format), tifffile (TIFF/LSM formats), Pillow (image I/O)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
